{
  "name": "thetokentown-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The Token Town — monorepo for the open-source CLI, core and source adapters.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ssssantiago/thetokentown.git"
  },
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "check": "pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
